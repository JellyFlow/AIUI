# 饮食推荐卡片

这是一个根据当前时间段推荐菜品的示例应用，使用本地 mock 数据展示单张饮食推荐卡片。

**核心能力：**

* **按时间段推荐**：基于当前本地时间自动判断早餐、午餐、晚餐或夜宵时段，并展示对应推荐菜品。
* **卡片化展示**：用紧凑卡片呈现推荐菜名、推荐理由、口味标签和建议搭配。
* **纯本地示例**：不依赖网络请求，便于演示 cut 页面与数据驱动渲染方式。

适合作为时间驱动内容展示与卡片 UI 的参考示例。
